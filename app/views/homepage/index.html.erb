<div class='map'>
  
</div>

<% if @users.any? %>

	<% @users.each do |user| %>
	  <h3><%= user.name_first %> <%= user.name_last %></h3>
	  <h4><%= user.job_title %></h4>
	  <p><%= user.location %></p>
	  <p><%= user.bio %></p>
	<% end %>

<% else %>
	<p>This is where users will show</p>
<% end %>